# وثيقة التسليم

`2026-09-02` · `RULE_OWNER = DR_HUSSEIN` · `MEASURED_NOT_PRESET`

مشروعان اشتُغل عليهما في هذه الجولة، ولكلٍّ منهما جهازُه ومستودعُه:

| المشروع | المسار | الحالة |
|---|---|---|
| `aslot` — السلسلة الخماسية | `~/final-september` | `2.11.0` ملتزَمٌ في git |
| `CL-16` — سلسلة الجذر والوزن | `~/hokom/cl16_root_wazn` | ثلاث مراحل، **ملتزَمةٌ** `30b389d` |

---

## القسم الأول — محرّك أسلوط

### ١ ـ١ ما كان وما صار

بدأت الجولة بخمسة سكربتات مسطّحة تقرأ `MASAQ.csv`. وصارت حزمةً واحدة
(`pip install -e .`، أمرُ `aslot`، `ruff` نظيف، ٤٣ اختبار `pytest`)،
وإعادةُ الهيكلة **بُرهن على حيادها بتطابقٍ بايتيّ في المخرجات الثمانية**
قبل أن يُغيَّر حكمٌ واحد.

ثم نُفِّذت **خمسةُ أحكام مالك**، كلٌّ منها بالنمط نفسه: قياسٌ **قبل** التنفيذ،
ثم قاعدةٌ مسمّاة وقائمةٌ مغلقة، ثم اختبارٌ مأخوذٌ من النصّ لا من لوحة المفاتيح،
ثم قياسُ الأثر، ثم التزامٌ يحمل نصَّ الحكم حرفيًّا.

| القاعدة | الحكم | الأثر المقيس |
|---|---|---|
| **ن٠-ج** | لفظ الجلالة يُمنع من كلّ إجراء | ٢٬٧٠٤ كلمة خرجت، بجردٍ مغلق من ١٦ سطحًا |
| **N8** | الخنجرية على الواو كالخنجرية على المقصورة | منفَّذة ومختبَرة، وشواهدُها في هذا المدخل **صفر** |
| **N9** | الشمسية تُحذف ويُكرَّر الحرف؛ والقمرية تبقى؛ واللام لا تُدغم | ٢٬٧٠٨ موضعًا |
| **N7.4** | `لِلنَّاسِ` = لام الجرّ + ال + ناس | ٣٢٥ موضعًا، **يوافق المرجعُ عليها كلَّها** |
| **N10.2** | «آ» رمزُ أداءٍ يُحذف بلا أثر، ويصير همزةَ استفهام | ٦ مواضع لا سابعَ لها |

### ١ ـ٢ الحالة المقيسة الآن  (مُعادُ قياسها على `2.9.0`)

⚠ **كان هذا القسمُ بائدًا** في `2.8.0`: نشر أرقامَ `2.6.0` تحت عنوان «الآن»
بعد ثلاث قواعدَ عامّةٍ وأربعِ مراحلِ امتثال. وهو خرقٌ لشرط «مقيسٌ لا منقول»
داخلَ الوثيقة التي تحرسه. أُعيد القياسُ كلُّه من جولةٍ واحدة:

```
فحوصٌ ذاتية   82/82        سموم        56/56
pytest        43            ruff        نظيف

NORMALIZED                          57,152
NORMALIZED_OWNER_DECISION_REQUIRED  17,516
EXCLUDED_LAFZ_AL_JALALAH             2,704
EXCLUDED_FAWATIH_AL_SUWAR               30
STOPPED_…_NO_CARRIER_FOR_A_HARAKA        9

فجوةُ المرجع (DET)  335 ← 10 ← 6       نمط CVVCC الشاذّ  5 ← 3 ← 1
ACTUAL_PEELS  17,252 ← 17,039 ← 9,630   STEMS  51,722 ← 51,573 ← 46,344
```

**وهذان الرقمان صمدا للقياس**: `NORMALIZED` و«تنتظر حكمًا» لم يتغيّرا بعد
`Elision_Points` و`canonical_mark_order` — والأولى تنشر عقدًا ولا تطبّع،
والثانيةُ تُرتّب ولا تغيّر حرفًا (صفرُ حرفٍ تغيّر، مقيس). فبقاؤهما ليس
نقلًا بل نتيجةُ إعادةِ قياس.

وما تغيّر هو التقشير، وسببُه معلنٌ في §١ـ٤.

### ١ ـ٤ ما رفعته مراجعتُك  (`2.9.0`)

خمسةُ بنودٍ في مراجعتك، أربعةٌ منها بلا حكمٍ جديدٍ منك:

| | ما كان | ما صار |
|---|---|---|
| `B1` | «الآن» أرقامُ `2.6.0` | مُعادُ قياسه كلُّه في §١ـ٢ |
| `B2` | CL-16 ملتزَمة ومعلَّقة معًا | البندُ البائد مشطوبٌ ومؤرَّخ |
| `B3` | ثلاثةُ فروقٍ وفرقُ ١٤٩ | مصفوفةٌ مغلقة: ٧٤٬٦٦٨ خانةً بلا بقيّة |
| `B4` | مقامٌ مجهول | ٨٢١ سطحًا مفحوصًا · **٠** مخالف |
| `كَتَبَ` | `ACCEPT` | `DEFER_REMAINDER_STANDING_UNPROVEN` |

**والخامس هو الأهمّ.** قلتَ: «القبولُ دعوى كالمنع سواء». وكان استدلالي أن
`P7` يمنع الجهلَ أن يكون **مانعًا** — وهو صحيح — ثم استنتجتُ منه القبول،
وذلك **لا يلزم**. فغيابُ الشاهد لا يمنع ولا يُرخِّص.

```
ACTUAL_PEELS  17,039 ← 9,630     ومنها قشورٌ صحيحة أُجّلت لا لأنها خطأ
                                  بل لأن دليلَها ليس بيدي
```

**ودفترُ الانتقالات يُغلق** بين `2.6.0` و`2.9.0`:

```
LEDGER_CLOSES = YES    74,668 = 74,668 = 74,668 خانةً، بلا بقيّة
NO_PROMOTION  = YES    لا صفَّ انتقل من تأجيلٍ أو حجبٍ إلى قبول
ثابت 66,370 (88.9%)    متحرّك 8,298 — كلُّه نزولٌ من دعوى إلى إقرار
```

`python3 scripts/transition_matrix.py <قديم> <جديد>` · يرجع `1` إن انكسرت أيُّهما.

**وجردُ الأسطح المكتوبة باليد**: ٨٢١ سطحًا في ٣٧ ملفًّا، **صفرٌ** مخالف في
`src/` و`tests/`. فالفحصُ الذي اصطيد كان الوحيدَ من صنفه. والثلاثون التي
تغيّرت كلُّها في `lafz_al_jalalah.json` — مولَّدٌ من النصّ، فترتيبُه ترتيبُه.

**وتعليلٌ باطلٌ صُحّح**: قلتُ إن الاستيراد «يجرّ محوِّلَ النموذج، وهو أوّلُ
سطرٍ في السطح الممنوع». و`ModelClient` بروتوكولٌ مجرَّد لا محوِّلٌ حقيقيّ.
والحجّةُ الصحيحة مقيسة: **٦ مثبَّتة مقابل ٨١ وحدةً تُنفَّذ** عند الاستيراد
من أعلى — فتثبيتُ ستٍّ وتنفيذُ إحدى وثمانين يُبطل معنى التثبيت.

**وواقعةٌ ثالثة كشفها التحقّق، وهي أثمنُ ما في هذه الجولة:**

بعد نشر `2.9.0` على جهازك أعطى الدفترُ `NO_PROMOTION = NO` — مصفوفةٌ مختلفة
عن السحابة. ولم يكن اختلافَ آلة: **`aslot all` كان يموت عند الاستيراد**،
والتقاريرُ التي قِستُ عليها من جولةٍ أقدم بأربع ساعات.

والعلّة: `taaqol` يستورد الحواملَ في **زمن استيراده**، و`cli` يستورد محورَ
الامتثال، فصار `import aslot.cli` يوقف **المحاورَ الخمسة كلَّها** على بايثون
٣٫١٠. وكنتُ قد كتبتُ «القيدُ على الجسر وحده» — قولٌ صحيحٌ في القصد **باطلٌ
في التنفيذ**.

وأسوأُ من العطل طريقةُ تحقّقي: عددتُ أسطرَ `[FAIL]` فوجدتُها صفرًا،
**وكان المخرجُ خاليًا من المحاور أصلًا**. فصفرُ الإخفاقات لم يكن دليلَ
سلامةٍ بل دليلَ أنّ شيئًا لم يجرِ. وهذا نظيرُ الفحص الذي كان يمرّ لعلّةٍ
خاطئة، إلا أنه في **إجراء التحقّق** لا في الاختبار.

العلاج: الحواملُ تُستورَد عند أوّل طلب لا عند الاستيراد. والحارس: اختبارٌ
يشغّل عمليّةً نظيفة ويثبت أن `import aslot.cli` لا يجرّ `taaqqul` — لأن
القياسَ في العملية الحالية يخدعه ما استُورد قبله.

```
جهازُك الآن يعيد إنتاج السحابة حرفًا بحرف:
peels 9,630 · stems 46,344 · LEDGER_CLOSES = YES · NO_PROMOTION = YES
```

**وبقايا لم تُرفع بعد:**

```
◻ R1  بصماتُ laws/ السبعة والحوامل الستّة غيرُ منشورةٍ في هذه الوثيقة
◻ R2  T-2 موسومةٌ «٦ مستوردة» ولا تقول إنها لا تُنفَّذ على ٣٫١٠
◻     دعوى «السجلّان يُغلقان بابَ كَتَبَ وحدَهما» ما زالت غيرَ مقيسة
```

و`native_stages = 16` (١٤ منفَّذة، ٢ لا) — **قياسُك أنت** لا قياسي، مُسنَدٌ
إليك ولم أنقله عن نفسي.

---

### ١ ـ٥ المراجعة الثانية  (`2.10.0`)

**الأهمّ: دعوى السجلّين مقيسةٌ الآن — ومحدودة.**

```
بالجرد المشتقّ (١٣٤)        يُحسم من الـ٦٬٢٣٠ =     0
بسجلَّي حُكم (٢٦٠ سطحًا)      يُحسم              = 1,646  (26.4%)
                             يبقى              = 4,584  (73.6%)
```

والصفرُ الأوّل **بنيةٌ لا فشلُ قياس**: الجردُ المشتقّ يقبل الكلمةَ التامّة
ذاتَ المقطع الواحد، فلا يحوي ضميرًا متّصلًا أبدًا — عاجزٌ بحدّه لا صامت.

فقولُ «السجلّان يُغلقان بابَ كَتَبَ وحدَهما» **مشطوبٌ**: يُغلقان الرُّبع.
وما يبقى جذوعٌ ومركّبات (`فَرُوْ` — بقيّةُ «كَفَرُوا» نفسِها — ١٨٩، `لَقَدْ`
١٢٤، `كَذَلِكَ` ٣٩) لا يُغلقها سجلُّ صورٍ مغلقة بحال.

| البند | الحال |
|---|---|
| ر-١ ٨٢١/٨٢٧ | المقامُ دالّةٌ في الشجرة؛ يُنشر مع بصمتها. والثابتُ **٠** |
| ر-٢ عزلُ `2.7.0` | الأرقامُ الثلاثة تُعاد حرفًا — **و١١ ترقيةً ظهرت** |
| ر-٣ دفترُ القشور | `PEEL_LEDGER_CLOSES` في الأداة نفسِها |
| ر-٤ جردُ الرفض | `declared 28 · mapped 7 · unmapped 21` |
| ص-١ البايتكود | **تشخيصٌ باطلٌ سُحب** — لا صنفَ يُحرَس |
| ص-٢ الإثبات الموجَب | `scripts/verify_run.py` |

**والأخطرُ ما كشفه عزلُ `2.7.0`**: رقّى **١١ صفًّا** من تأجيلٍ إلى قبولٍ بلا
دليلٍ جديد، منها `كَبَائِرَ` — **شاهدُ البوابة المسمّى**. والفروقُ المنفصلة
أخفتها لأنها تُظهر الصافيَ لا الحركة. و`2.9.0` أزالها **عرَضًا لا قصدًا**.

**وص-١ سحبٌ لا رفع**: قلتُ إن النشر حفظ أزمنةً أقدمَ من `.pyc`. وذلك باطل —
`cp -R` يضع زمنَ النسخ، فالمصدرُ كان أحدثَ دائمًا. والعلّةُ الحقيقية موتُ
`aslot all` عند الاستيراد. وسحبُ تشخيصٍ باطلٍ أولى من بناء حارسٍ عليه؛
والصنفُ الحقيقيّ يسدّه `verify_run.py` أيًّا كانت العلّة.

---

### ١ ـ٦ تقريرُ الفحص الدستوريّ — مقيسًا  (`2.11.0`)

قلتَ: `OPERATORS_CSV` و`ASLOT_ENGINE` والمخرجاتُ عندي لا عندك، فالمولِّدُ
يُشغَّل هنا. فشُغِّل، وهذه أرقامُه — لا مسوّدتُه.

**١ · `١٠٧` أم `١٦٠`؟ — الجوابُ مقيس.**

```
hokom/data/operators_catalog_split_vocalized_corrected.csv
    ١٦٠ صفًّا · ١٥٣ عاملًا فريدًا · ١٦٠ بشاهدٍ مشكول
    sha256 = 043ed5f364bd…   وفيه عمودٌ زائد: is_operator
الملفّان غيرُ المصحَّحين                   ١٠٢ صفًّا
١٠٧                                        لا يطابق ملفًّا واحدًا
```

فـ`١٦٠` هو المصحَّح، و`١٠٢` غيرُ المصحَّح، و`١٠٧` **لا أصلَ له في أيّ ملفّ**
على جهازك. وقد استُعمل المصحَّح، وبصمتُه مطبوعةٌ في التقرير.

**٢ · لِمَ لم يكن المرفوعُ تقريرًا دستوريًّا — وبمَ صار.**

كان أرقامًا بلا سند. والآن كلُّ رقمٍ يحمل رقاقتَه، وقبل أن يُقرأ نفيٌ واحد
يمرّ الموجَب. وثلاثةُ مثبَّتات:

| | المثبَّت | مصدرُه | المحاور ٠→٤ |
|---|---|---|---|
| F1 | البقرة ٢٨٢ | نصٌّ قرآنيّ | `129 · 129 · 123 · 123 · 123` |
| F2 | شواهدُ جدولك | **مشتقٌّ من عمود `Example_Vocalized`** لا مكتوبٌ بيد | `457 · 457 · 446 · 446 · 446` |
| F3 | نثرٌ حديث | خارجُ المصحف | `157 · 157 · 133 · 133 · 133` |

```
TRACE   3,592 صفًّا · بمرساة 3,592 · بلا مرساة 0 · أُعيد بناؤها وطوبقت 3,592
MARK_ORDER_VERIFIED = YES   717 سطحًا · 0 مخالفة
```

**٣ · «الجردُ المختلط» و«التخطّي الصامت» — سُدّا بقاعدتين لا بترقيع.**

*الجردُ* يُقسم ثلاثًا بمرجعٍ مُعلَن (`ASLOT_REFUSALS`) لا بتصنيفٍ من عندي:
`WITNESSED` مُعلَنٌ وله شاهد · `UNWITNESSED` مُعلَنٌ بلا شاهدٍ هنا ·
`UNDECLARED` ظهر ولم يُعلَن.

*والتخطّي* يُسدّ بدفتر حملٍ يقفل على كلّ حافّة: `دخل = حُمل + مسمًّى + بلا
اسم`. والاثنا عشر دفترًا تقفل، و**بلا اسمٍ = ٠**:

```
F1  1→2   129 = 123 + EXCLUDED_LAFZ_AL_JALALAH 6
F2  1→2   457 = 446 + EXCLUDED_LAFZ_AL_JALALAH 7 + IGNORED_NON_WORD_TOKEN 4
F3  1→2   157 = 133 + IGNORED_NON_WORD_TOKEN 24
```

**وما كشفه هذا — وهو الأهمّ في الجولة.** `UNDECLARED = 2`:
`IGNORED_NON_WORD_TOKEN` و`NON_LETTER` كانا **يخرجان في مخرجاتٍ فعليّة ولا
يُعلَنان**. ولم يظهرا في جرد النصّ القرآنيّ لأنّه بلا ترقيم — فكشفهما نثرُ
`F3`. والثاني أسوأُ: لم يكن اسمًا معرَّفًا أصلًا، بل حرفًا داخل نصٍّ منسَّق.

والعلاجُ عامّ: `STOP_REASON_FAMILIES` جردٌ مغلقٌ لستَّ عشرةَ أسرة، و
`T_EVERY_STOP_REASON_FAMILY_IS_DECLARED` يفحص **ما خرج فعلًا** فيُسقط الجولةَ
إن أُضيفت أسرةٌ في الكود ولم تُعلَن. و`ASLOT_REFUSALS` صار ٣٠.

**٤ · تغطيةُ عواملك — في شاهدِ كلٍّ منها، لا في المجموع.**

```
PROVEN_IN_ITS_OWN_WITNESS     31
PRESENT_BUT_NOT_PROVEN        73
NOT_A_STANDALONE_TOKEN        56      LICENSE_GRANTED = NO
```

والثالثةُ ليست إخفاقًا: `بِ` في `بِزَيْدٍ` **ليست توكنًا قائمًا**، والمحورُ
الثاني جردُ كلماتٍ تامّةٍ مغلقة. فعدُّها إخفاقًا خلط، وعدُّها نجاحًا دعوى —
فسُمّيت وتُركت. وهذا هو الصنفُ الذي أفسد دعوى السجلّين قبلها.

**وخطأٌ وقعتُ فيه في أوّل تشغيلٍ وسُجّل**: عدّت البوّابةُ الأسطرَ ببادئة
`DEVIATION`، و`DEVIATIONS 0` تبدأ بها — فقلبت صفرًا إلى ثلاثٍ وأسقطت الجولةَ
بحكم `BLOCK`. وهي الواقعةُ نفسُها في صورةٍ ثالثة: فحصٌ يُصيب لعلّةٍ خاطئة.
والعلاجُ عامّ: تُقرأ الخلاصةُ المُعلَنة، ويُشترط وجودُها، ولا تُستنبط بعدّ
بادئات.

| ما لا يدّعيه هذا التقرير | لماذا |
|---|---|
| أحكامَ الغلق الستّة على الصفوف | Γ لم يُشغَّل — T-5 |
| رتبةً محمولةً في مخرج | T-7 |
| اشتعالَ خطٍّ ممنوع | السجلُّ لا يُستشار وقتَ التشغيل — T-6 |
| صنفَ بقيّةٍ واحدًا | `P4` — حكمُك |

```
scripts/prepare_inspection_inputs.py     يشتقّ مدخلات المثبَّتات آليًّا
scripts/build_taaqol_inspection.py       يُشغَّل فيقيس فيولّد
inspection/taaqol_inspection.html        المخرج
```

---

### ١ ـ٣ ما ينتظر حكمك — مرتَّبًا بالأثر

**وهذه هي القائمةُ الجامعة، بعد ثلاث جولات:**

```
◻ T-4  صنفُ البقيّة للأصناف السبعة   ← يفتح سقوفَ الرتبة كلَّها
◻ T-4  عيبُ التنوين (٣٬٠٥٩): حجبٌ أم بقيّةٌ ظاهرةٌ مؤجَّلة؟
◻      السجلّان (عوامل ١٦٠ · مبنيات ٥٦٥) ← يُغلقان بابَ «كَتَبَ» وحدَهما
◻ T-5  تشغيلُ الغلق Γ — يغيّر أحكامًا
◻ T-6  الخطوطُ الممنوعة — تُنزل تقشيرَ «كَتَبَ» عن ACCEPT
◻ T-7  البوّابات بين المحاور
◻ T-8  حذفُ النسخة المكرّرة maqayis_v2 داخل hokom
◻      «آ» خارج «أل»  ·  OPEN_QUESTION_N2_2  ·  «لكم» في CL-16
◻      إضافةُ محوِّل المسار للمرحلتين ١ و٢ في CL-16
```


1. **عيبُ التنوين — ٣٬٠٥٩ كلمة.** أكبرُ خللٍ قائم. المعالجةُ المؤقّتة تفكّ
   التنوين إلى حركةٍ + نونٍ ساكنة ولا تحذف الألف الصامتة قبله في تنوين الفتح،
   فيولد **صامتٌ زائد**: `شَيْئَاً ← شَيْئَاَنْ` بخمسة صوامت لا أربعة،
   والمحور الثالث يقبله ولا يشكو.
   الصيغة المعروضة: «تنوينُ الفتح على ألفٍ صامتة ⟵ تُحذف الألف ويلحق التنوينُ ما قبلها».

2. **سبعةُ أصنافٍ غير مصادَقة** في `data/axis_1_owner_policy.json`. التصديقُ
   تغييرُ `false` إلى `true` في JSON، **لا تعديلُ سطرٍ في محرّك**:
   `U_TANWEEN` ٨٬٨٩٤ · `U_ALIF_FARIQA` ٣٬٥٦١ · `U_ALIF_MAQSURA` ٢٬٤٩٨ ·
   `U_N7_2_INTERNAL_AL` ١٬٦١٦ · `U_ALEF_MADDA` ١٬٥٠٥ · `U_UNVOCALIZED_CARRIER` ٦٠ ·
   `U_MULTIWORD_CELL` ٩.

3. **همزةُ الاستفهام الممدودة على غير «أل»** — صنفٌ ظهر أثناء N10.2 ولم يُحكم فيه.

4. **`OPEN_QUESTION_N2_2`** — وسمُ الشدّة بعد «ال»، ثلاثُ صيغٍ معروضة بلا اختيار.

5. **سجلّا المحور الثاني**: `operators_catalog` (١٦٠ مدخلة) و`mabniyat_full`
   (٥٦٥ مدخلة)، **مشكولان كما في النصّ**. الجردُ الحاليّ شاهدٌ مشتقّ (١٣٤ مدخلة)
   لا سجلُّ مالك، و`LICENSE_GRANTED = NO`.

6. **توسيعُ N10.2 إلى «آ» كلِّها؟** لم أوسّع: علّتُك («ء استفهام») لا تصدُق حيث
   لا استفهام، والتوسيعُ يُسقط المدَّ من ١٬٤١٨ موضعًا معجميًّا (`آمَنُوا ← ءَمَنُوا`).

---

## القسم الثاني — CL-16: سلسلة الجذر والوزن

### ٢ ـ١ المعمار — بحكمك

> «ليس أن ننقل كل هذه المشاريع داخل Hokom، ولا أن نغيّر Taaqol.
> الصحيح أن نجعلها مصادر أدلة مجمّدة: نثبت ملفاتها المهمة بالبصمة،
> ثم يقرأها Hokom عبر وسيط صغير، ويمنع أي ترقية غير مأذونة.»

كان قائمًا: المرحلةُ الأولى (اعتماد الجذر والباب) والثانية (تمرير الوزن)،
و**١٨ بصمةً كلُّها سليمة**. وكان الناقص أنّ المرحلة الأولى مفتاحُها **جذر**،
ولم يكن في CL-16 ما يُخرج جذرًا من سطح — رأسٌ بلا عنق.

### ٢ ـ٢ ما أُنجز: المرحلة الثالثة

`hokom/cl16_root_wazn/phase3/` — سلسلةٌ من ثلاث حلقات، كلُّ مصدرٍ في حدّه:

```
مرشح (أبو فريد) ← وجود (مقاييس) ← اعتماد وباب (audited_roots)
```

وسؤالُ مصدرٍ عمّا يمنعه حدُّه **حجبٌ لا تأجيل**: التأجيلُ إقرارٌ بحجّةٍ ناقصة،
وهذا خطأُ صنفٍ من أصله.

```
CORROBORATION_NEVER_RAISES_RANK
    الرتبةُ من جدول الاعتماد وحده. الترشيحُ والوجودُ تعضيدٌ: لكلٍّ أن يُنزل
    الحكمَ إلى تأجيل، وليس لأيٍّ منهما أن يرفع رتبةً درجةً واحدة.

SKELETON_MATCH_IS_NOT_A_ROOT_PROOF
    هيكلٌ صامتيّ يوافق صورةً في جرد Hokom المغلق: تأجيلٌ لا ترخيص.
```

**القياس على ٧٠٠ مدخلة**:

```
331 أجّله المستخرِج · 200 ليس في مقاييس · 67 بلا باب
 33 تصادمٌ مغلق     ·  33 أبوابٌ متعدّدة ·  23 غير معتمَد
 13 مرخَّص
```

المرخَّصةُ الثلاثَ عشرةَ: `غضب فعل فقد فهم قتل قول قوم كذب كلم` (مُثبَتة)
و`لحق لكم نصر نظر` (مدعومةٌ بدليل) — كلٌّ ببابه.

`17 اختبارًا يمرّ`. الحدود: `TAAQOL_CHANGED = NO` · `WEIGH_CALLED = NO` ·
`REPO_COPIED = NO` · `ROOT_PROVEN = NO` · `CL16_CLOSED = NO`.

### ٢ ـ٣ ما ينتظر حكمك

1. **`لكم` يُرخَّص وهو ليس جذرًا هنا**: أصلُه `لَكُمْ` (لام + ضمير)، وفي الجدول
   `لَكَمَ` (الضرب). الجردُ المغلق لا يمسكه لأن `لكم` فيه جزآن لا صورةٌ واحدة.
   **لم أخترع له قاعدة**؛ سُجِّل صنفًا مسمّى.

2. **نسخةٌ مكرّرة داخل Hokom**: `hokom/pipeline/taaqol_integration/maqayis_v2/`
   فيه المشروع كاملًا، وقاعدتُه مطابقةٌ بالبصمة للأصل **اليوم** (`35c7062dd5ed`).
   هي عينُ ما نهيتَ عنه، وقد تنحرف صامتةً غدًا.

3. **مُحوِّل `maqayis_v2` يفشل مفتوحًا** — «خطأ في المعجم لا يوقف الـ pipeline
   أبداً» — نقيضُ الفشل المغلق. ثُبِّت شاهدًا ولم يُستورد.

4. **المرحلتان ١ و٢ لا تُراجَعان إلا على جهازك**: مساراتُهما مطلقةٌ بلا تصريحٍ
   بمُركَّبٍ بديل. أُضيف المُحوِّل في الثالثة (`CL16_SOURCE_ROOT`، معطَّلٌ افتراضًا
   ومُثبَتٌ باختبار). وإضافتُه إليهما تعديلٌ في مرحلةٍ مشحونة — فهو حكمُك.

5. ~~`cl16_root_wazn/` غيرُ متتبَّع، وقفلُ `index.lock` عالق~~ — **رُفع**:
   أُزيل القفلُ بإذنك والتُزمت المراحلُ الثلاث في `30b389d`. وكان بقاءُ هذا
   البند مع رأس الوثيقة تناقضًا داخلها، ولا يُترك الحكمُ للقارئ.

---

## القسم الثالث — مثالٌ كامل: `كَتَبَ زَيْدٌ بِالْقَلَمِ`

جملةٌ واحدة مرّت على المحاور الخمسة. **كلُّ ما يلي مخرَجُ تشغيلٍ فعليّ**، منسوخٌ
كما طُبع، لا مُصاغٌ باليد.

```bash
printf '1|1|كَتَبَ زَيْدٌ بِالْقَلَمِ\n' > sentence.txt
aslot corpus --text sentence.txt --fragment --output-dir demo/axis0
aslot normalize --words demo/axis0/QURAN_WORDS.csv       --output-dir demo/axis1
aslot registry  --axis1-csv demo/axis1/AXIS_1_NORMALIZATION.csv --output-dir demo/axis2
aslot syllabify --axis1-csv demo/axis1/AXIS_1_NORMALIZATION.csv --output-dir demo/axis3
aslot peel      --axis1-csv demo/axis1/AXIS_1_NORMALIZATION.csv \
                --axis3-csv demo/axis3/AXIS_3_SYLLABLES.csv \
                --output-dir demo/axis4 --emit-masaq-like
```

### المحور ٠ — بناء النصّ · `QURAN_WORDS.csv`

```
Sura_No,Verse_No,Word_No,Word,Segment_Count,Segment_Numbers,Surface_Conflict,Segment_Numbering_Gap
1,1,1,كَتَبَ,1,1,NO,NO
1,1,2,زَيْدٌ,1,1,NO,NO
1,1,3,بِالْقَلَمِ,1,1,NO,NO
```

`FOLD_IS_LOSSLESS = YES` · فحوصٌ ٤/٤ · سمومٌ ٧/٧

### المحور ١ — التطبيع · `AXIS_1_NORMALIZATION.csv`

```
Sura_No,Verse_No,Word_No,Word,Normalized_Word,Normalization_Status,Owner_Decision_Classes,Rules_Applied,Stop_Reason,Jalalah_Prefix,Jalalah_Preserved
1,1,1,كَتَبَ,كَتَبَ,NORMALIZED,,,,,
1,1,2,زَيْدٌ,زَيْدُنْ,NORMALIZED_OWNER_DECISION_REQUIRED,U_TANWEEN,N12,,,
1,1,3,بِالْقَلَمِ,بِلْقَلَمِ,NORMALIZED_OWNER_DECISION_REQUIRED,U_N7_2_INTERNAL_AL,N7.2|N12,,,
```

* `كَتَبَ` لم تُمَسّ — لا قاعدةَ لها عمل.
* `زَيْدٌ ← زَيْدُنْ` بالتنوين المفكوك. **وهنا لا صامتَ زائد** لأن التنوين ضمٌّ
  على صامت؛ والعيبُ إنما يقع في تنوين الفتح على ألفٍ صامتة.
* `بِالْقَلَمِ ← بِلْقَلَمِ` — حُذفت همزةُ الوصل بـN7.2. واللامُ قمريّة (قاف)
  فبقيت ساكنة. ولو كانت شمسيّةً لعملت N9: `بِالشَّمْسِ ← بِشْشَمْسِ` (مقيس).

### المحور ٢ — العوامل والمبنيات · `AXIS_2_TOKENS.csv`

```
Sura_No,Verse_No,Word_No,Word,Normalized_Word,Closed_Form_Proof,Eligibility,Operator_Role,Word_Class,Next_Route,Matched_Entry_Ids,Note
1,1,1,كَتَبَ,كَتَبَ,NOT_MATCHED,BLOCK,NO,,CONTINUE,,
1,1,2,زَيْدٌ,زَيْدُنْ,NOT_MATCHED,BLOCK,NO,,CONTINUE,,
1,1,3,بِالْقَلَمِ,بِلْقَلَمِ,NOT_MATCHED,BLOCK,NO,,CONTINUE,,
```

لا شيءَ من الثلاث صورةٌ مغلقة، فالثلاثُ تمضي (`CONTINUE`).

### المحور ٣ — المقاطع الصوتية · `AXIS_3_SYLLABLES.csv`

```
Sura_No,Verse_No,Word_No,Word,Normalized_Word,Verdict,Syllable_Pattern,Consonant_Count,Madd_Status,Layn_Status,Reconstruction_Verified,Boundaries,Block_Reason
1,1,1,كَتَبَ,كَتَبَ,ACCEPT,CV·CV·CV,3,,,YES,0|2|4|6,
1,1,2,زَيْدٌ,زَيْدُنْ,ACCEPT,CVC·CVC,4,,PROVEN,YES,0|4|8,
1,1,3,بِالْقَلَمِ,بِلْقَلَمِ,ACCEPT,CVC·CV·CV·CV,5,,,YES,0|4|6|8|10,
```

الثلاثُ مقبولة، وكلُّها `Reconstruction_Verified = YES` — أي أُعيد بناءُ السطح
من مقاطعه وطوبق. و`زَيْدٌ` وحدها فيها لِينٌ مُثبَت (`Layn_Status = PROVEN`):
الياءُ الساكنة بعد فتحةٍ **صامتٌ في القفل لا امتدادُ نواة**.

### المحور ٤ — التقشير · `AXIS_4_PEEL_TO_STEM.csv`

```
Sura_No,Verse_No,Word_No,Word,Normalized_Word,Verdict,Termination,Peel_Count,Peeled_Prefixes,Licenses,Stem_Surface,Stem_Pattern,Stem_Consonants,Closed_Form_Proof,Deferred_Candidate_Prefix,Root_Work,Root_Proven,Stem_Proof,Note
1,1,1,كَتَبَ,كَتَبَ,ACCEPT,STEM_NOT_FURTHER_PEELABLE,1,كَ,LIC:4,تَبَ,CV·CV,2,NOT_MATCHED,,NONE,NO,NOT_CLAIMED,
1,1,2,زَيْدٌ,زَيْدُنْ,ACCEPT,STEM_NOT_FURTHER_PEELABLE,0,,,زَيْدُنْ,CVC·CVC,4,NOT_MATCHED,,NONE,NO,NOT_CLAIMED,
1,1,3,بِالْقَلَمِ,بِلْقَلَمِ,BLOCK,BLOCK_SYLLABLE_BOUNDARY_CROSSED,0,,,,,0,NOT_MATCHED,,NONE,NO,NOT_CLAIMED,القطع عند 2 يعبر حدًّا مقطعيًّا (CVC·CV·CV·CV)
```

### الجدول الجامع · `MASAQ_LIKE_OUTPUT.csv`

```
ID,Sura_No,Verse_No,Word_No,Segment_No,Word,Normalized_Word,Segment_Surface,Segment_Role,Peel_License,Syllable_Pattern,Consonant_Count,Closed_Form_Proof,Verdict,Termination,Morph_Type,Root
1,1,1,1,1,كَتَبَ,كَتَبَ,كَ,PREFIX,LIC:4,,,,ACCEPT,STEM_NOT_FURTHER_PEELABLE,,
2,1,1,1,2,كَتَبَ,كَتَبَ,تَبَ,STEM,,CV·CV,2,NOT_MATCHED,ACCEPT,STEM_NOT_FURTHER_PEELABLE,,
3,1,1,2,1,زَيْدٌ,زَيْدُنْ,زَيْدُنْ,STEM,,CVC·CVC,4,NOT_MATCHED,ACCEPT,STEM_NOT_FURTHER_PEELABLE,,
4,1,1,3,1,بِالْقَلَمِ,بِلْقَلَمِ,بِلْقَلَمِ,WHOLE_WORD,,CVC·CV·CV·CV,5,NOT_MATCHED,BLOCK,BLOCK_SYLLABLE_BOUNDARY_CROSSED,,
```

### ٣ ـ١ ماذا كشفت هذه الجملة — وهي ثلاثُ كلمات

**١. `كَتَبَ` قُشِّرت خطأً إلى `كَ + تَبَ`.**
الكافُ أصلٌ في «كتب» والمحرّك قشّرها. وهذا هو الحدُّ المسجَّل نفسُه
`T3_KNOWN_LIMIT_KAFARU_IS_WRONGLY_PEELED` (شاهدُه `كَفَرُوا`)، إلا أن وقوعَه هنا
**على الفعل النموذج نفسِه** أشدُّ دلالةً من شاهده الأصليّ.
والسبب مقيس: بوابةُ `INITIAL_LETTER_MAY_BE_RADICAL` تشترط نمط `CVV·CV·CV`،
و`كَتَبَ` نمطُها `CV·CV·CV` فلا تلتقطها البوابة. **حدٌّ معروف لا نقضٌ للمانع** —
والصنفُ أوسعُ من بوابته، وهذا مصرَّحٌ به في `README §٦`.

**٢. `بِالْقَلَمِ` حُجبت بالكامل ولم تُقشَّر.**
الباءُ سابقةٌ مرخَّصة (`بِ` في القائمة المغلقة)، ولكن N7.2 حذفت همزةَ الوصل
فالتصقت الباءُ باللام الساكنة وصار أوّلُ مقطعٍ `بِلْ` = `CVC`. والقطعُ عند
الموضع ٢ يعبر هذا المقطع، فالحكم `BLOCK` لا ترخيص. أي إنّ **قاعدةَ تطبيعٍ
غيرَ مصادَقة (`U_N7_2_INTERNAL_AL`، ١٬٦١٦ كلمة) تُسقط التقشير في المحور الرابع**.
وهذه كلفةٌ لم تكن ظاهرةً في الأرقام الإجمالية.

**٣. لا جذرَ ولا وزن — بالتصريح لا بالعجز.**
`Root` فارغ، و`Root_Work = NONE`، و`Root_Proven = NO`، و`Stem_Proof = NOT_CLAIMED`.
مسارُ الجذر مغلقٌ بحكمك، والجذعُ **وسمُ موقفٍ لا دعوى صرفية**.

### ٣ ـ٢ الوصلُ بـ CL-16 — ماذا يقول جذرُ «كتب»؟

المحور الرابع لا يخرج جذرًا. فإن سُئلت سلسلةُ CL-16 عن `كتب`:

```
كتب   | DEFER | MULTI_BAAB | rank=CANDIDATE | root=كتب | baab=None
   └─ ROOT_CANDIDATE_SOURCE    ROOT_CANDIDATE(NOT_A_PROVEN_ROOT)
   └─ MAQAYIS_LEXICON_SOURCE   PRESENT(PRESENCE_ONLY)
   └─ ROOT_PROOF_TABLE         DEFER:MULTI_BAAB
```

عبر الحلقاتِ الثلاثَ كلَّها ثم وقف عند الباب. والسبب في جدول الاعتماد:

```
4065  كَتَبَ | فَعَلَ يَفْعُلُ ( فتحُ ضمٍّ )   | مشترك | صحيح=1 أُعيد=1
4066  كَتَبَ | فَعَلَ يَفْعِلُ ( فتحُ كَسْرٍ ) | مشترك | صحيح=1 أُعيد=1
```

الجذرُ **مُثبَتٌ** رتبةً، وله **بابان**. فالوسيطُ لا يختار أحدَهما ولا يرجّح:
`DEFER` لا `BLOCK` — إقرارٌ بأنّ الحجّة ناقصة، لا دعوى بوجود مانع.
ولو سُئل عن `قتل` لرخّص: `CERTIFIED` ، `فَعَلَ يَفْعُلُ ( فتحُ ضمٍّ )`.

و`قلم` و`زيد` ليسا في جدول المرشحات أصلًا (`NOT_A_CANDIDATE`) — لأن المستخرِج
دُرّب على النصّ القرآنيّ، و`زيد` ليس منه.

---

## القسم الثالث ـ٢ — ثلاثُ قواعد عامّة  (`2.7.0`)

بحكمك: «صلح جميع الاخطاء بقاعدة عامة وليس اصلاح محلي». فصِيغت العلّةُ نفسُها
في كلّ موضعٍ بدل ثلاثة ترقيعات:

| الخطأ | الترقيعُ المرفوض | القاعدة |
|---|---|---|
| سطحٌ مكتوبٌ باليد لا يطابق النصّ | «انقُل من المصدر» — يعتمد على انضباط الكاتب | توحيدُ ترتيب العلامات عند الحدّ (`canonical_mark_order`) |
| `بِالْقَلَمِ` تُحجَب | استثناءُ الباء أو تليينُ الحدّ المقطعيّ | عقدُ الغياب المسمّى (`Elision_Points`) |
| `كَتَبَ ← كَ + تَبَ` | إضافةُ نمطٍ آخر إلى البوابة | البقيّةُ تقوم بنفسها أو لا تُقشَر |

```
١٨٬٥١٤ كلمة أُعيد ترتيبُ علاماتها · صفرُ حرفٍ تغيّر
BLOCK_SYLLABLE_BOUNDARY_CROSSED     4,920 ← 3,017
DEFER_ELIDED_LETTER_NOT_RESTORABLE      — ← 1,885
DEFER_INITIAL_LETTER_MAY_BE_RADICAL    16 ←   183
```

**وأوّلُ ما كشفته القاعدةُ الأولى**: الفحص `T1_ALLAH_SHADDA_UNFOLDS` كان
**يمرّ لعلّةٍ خاطئة منذ كُتب** — سطحُه المكتوب باليد لم يكن يطابق جردَ لفظ
الجلالة فيسلك مسارَ التطبيع العامّ. فالترقيعُ الموضعيّ لا يُخفي الخطأ وحده،
بل يُبقي الاختبارَ الذي كان يجب أن يكشفه شاهدَ زورٍ يمرّ في كلّ جولة.

**و«كَتَبَ» ما زالت تُقشَّر** — إقرارٌ لا تقصير: بقيّتُها قصيرة والسجلُّ قد
يحويها، فالحكمُ اليومَ مجهول، وادّعاءُ المنع دعوى بلا دليل. والسجلّان
يُغلقان الباب في سطرٍ واحد.

---

## القسم الثالث ـ٣ — الامتثال لتعقُّل  (`2.8.0`)

مستودعُ تعقُّل مُنزَلٌ في `vendor/Taaqol-GPT` (`Apache-2.0`، الالتزام
`3cccdded7951`) ويُستورَد منه مباشرةً. نُفِّذت **أربعُ مراحل**، وهي التي
نصّت الخطّةُ نفسُها على أنها تُنفَّذ بلا حكمٍ جديد:

| | الدليل المقيس |
|---|---|
| `T-0` تجميدُ القوانين | `laws/` — ٧ ملفّاتٍ ببصماتها، ولا سطرَ كود |
| `T-1` جردُ الرفض | ٢٧ اسمًا من مخرجاتٍ فعليّة (٧ لها مقابل، ٢٠ بلا — معلنة) |
| `T-2` حواملُ النواة | ٦ مستوردةً ومثبَّتةَ البصمة، **ولا سابعَ يُنفَّذ** |
| `T-3` مرساةُ الأثر | **٣٧٨٬٨٢٦ صفًّا** · بلا مرساة = **٠** |

```bash
git clone --depth 1 https://github.com/sonaiso/Taaqol-GPT vendor/Taaqol-GPT
aslot compliance        # جدولُ الامتثال كلُّه، مقيسًا  (بايثون ٣٫١١+)
```

**واقعتان كشفهما التشغيل:**

١. استيرادُ حاملٍ بسيط كان يجرّ معه **محوِّلَ النموذج** — أوّلُ سطرٍ في السطح
الممنوع كلّيًّا. فصار يُنفَّذ من المستودع ستّةٌ لا سابعَ لها. ولولا ذلك لَما
كان لتثبيت ستّ بصماتٍ معنى.

٢. حواملُ تعقُّل تحتاج **بايثون ٣٫١١**؛ وأسلوطُ نفسُه يعمل على ٣٫١٠، فقُيّد
الجسرُ وحدَه ووقف بإنذارٍ مفهوم، وفُصلت مرساةُ الأثر إلى `aslot/trace.py`.

**تصحيحاتٌ على أرقام الخطّة** (وقد صرّحت بأنها منقولةٌ لا مُعادُ قياسها):

| | الخطّة | المقيس |
|---|---:|---:|
| هدفُ `rows_with_trace_anchor` | 57,152 | **378,826** |
| `failure_codes` عند تعقُّل | 146 | **92** |
| `hidden_residual_count` | 3,059 | **3,059** ✓ |
| `native_stages` | 16 | **UNMEASURED** |

وعيبُ التنوين قِستُه أضيقَ من الصنف: **٣٬٠٥٩** لا ٨٬٨٩٤ — تنوينُ الفتح على
ألفٍ صامتة وحدَه. وخلطُ الصنف بالخلل تضخيمٌ يفسد الحكم.

### ما لم يُنفَّذ ولا يُدَّعى

```
closure_states_in_use    = 0    Γ لم يُشغَّل، والأحكامُ لم تتغيّر   (T-5)
residual_kinds_assigned  = 0/7  البقايا بلا صنفٍ ولا سقف          (T-4)
gates_between_axes       = 0    الانتقالُ استدعاءٌ مباشر           (T-7)
forbidden_lines_enforced = 0    تقشيرُ «كَتَبَ» ما زال ACCEPT       (T-6)
```

وسمٌّ يمنع ادّعاءها: `P4_RESIDUAL_KINDS_ARE_NOT_SELF_ASSIGNED` يسقط إن
أُسنِد صنفُ بقيّةٍ واحد.

---

## القسم الرابع — كيف يُستأنف العمل

```bash
# أسلوط
cd ~/final-september
pip install -e ".[dev]"     # أو  export PYTHONPATH=src
aslot all                   # السلسلة كاملةً على MASAQ.csv + قياسُ الامتثال
make test && make lint

# CL-16
cd ~/hokom/cl16_root_wazn/phase3
python3 measure_chain.py --json PHASE3_MEASURES.json
python3 -m pytest tests/ -q
```

### المبادئ التي حكمت الجولة — وينبغي ألّا تُخرق

```
OWNER_RULE_SOVEREIGNTY = ABSOLUTE     CREATE_NON_OWNER_RULE = NO
INFER_LINGUISTIC_RULE  = NO           MEASURED_NOT_PRESET   = دائمًا
ACCEPT = علّةٌ حاضرة ∧ شروطٌ مستوفاة ∧ لا مانع
DEFER ≠ BLOCK: الحجبُ يدّعي مانعًا، والتأجيلُ يقرّ بحجّةٍ ناقصة
```

و**درسٌ مسجَّلٌ يستحقّ التكرار**: أوّلُ جردٍ للفظ الجلالة كُتب باليد فلم يطابق
منه إلا سطحٌ واحد من ٢٬٧٠٤ — لأن النصّ يكتب الشدّة قبل الحركة واليدُ تعكسهما.
فالقاعدة: **كلُّ سطحٍ مشكولٍ مكتوبٍ في ملفّ كودٍ أو اختبار غيرُ موثوقٍ بطبعه،
وما لم يُنقَل من مصدره فهو دعوى.**
