# وثيقة التسليم

`2026-09-02` · `RULE_OWNER = DR_HUSSEIN` · `MEASURED_NOT_PRESET`

مشروعان اشتُغل عليهما في هذه الجولة، ولكلٍّ منهما جهازُه ومستودعُه:

| المشروع | المسار | الحالة |
|---|---|---|
| `aslot` — السلسلة الخماسية | `~/final-september` | `2.6.0` ملتزَمٌ في git |
| `CL-16` — سلسلة الجذر والوزن | `~/hokom/cl16_root_wazn` | ثلاث مراحل على القرص، **غيرُ ملتزَمة** |

---

## القسم الأول — محرّك أسلوط

### ١ ـ١ ما كان وما صار

بدأت الجولة بخمسة سكربتات مسطّحة تقرأ `MASAQ.csv`. وصارت حزمةً واحدة
(`pip install -e .`، أمرُ `aslot`، `ruff` نظيف، ٤٣ اختبار `pytest`)،
وإعادةُ الهيكلة **بُرهن على حيادها بتطابقٍ بايتيّ في المخرجات الثمانية**
قبل أن يُغيَّر حكمٌ واحد.

ثم نُفِّذت **خمسةُ أحكام مالك**، كلٌّ منها بالنمط نفسه: قياسٌ **قبل** التنفيذ،
ثم قاعدةٌ مسمّاة وقائمةٌ مغلقة، ثم اختبارٌ مأخوذٌ من النصّ لا من لوحة المفاتيح،
ثم قياسُ الأثر، ثم التزامٌ يحمل نصَّ الحكم حرفيًّا.

| القاعدة | الحكم | الأثر المقيس |
|---|---|---|
| **ن٠-ج** | لفظ الجلالة يُمنع من كلّ إجراء | ٢٬٧٠٤ كلمة خرجت، بجردٍ مغلق من ١٦ سطحًا |
| **N8** | الخنجرية على الواو كالخنجرية على المقصورة | منفَّذة ومختبَرة، وشواهدُها في هذا المدخل **صفر** |
| **N9** | الشمسية تُحذف ويُكرَّر الحرف؛ والقمرية تبقى؛ واللام لا تُدغم | ٢٬٧٠٨ موضعًا |
| **N7.4** | `لِلنَّاسِ` = لام الجرّ + ال + ناس | ٣٢٥ موضعًا، **يوافق المرجعُ عليها كلَّها** |
| **N10.2** | «آ» رمزُ أداءٍ يُحذف بلا أثر، ويصير همزةَ استفهام | ٦ مواضع لا سابعَ لها |

### ١ ـ٢ الحالة المقيسة الآن

```
NORMALIZED                         57,152      تنتظر حكمًا   17,516
فحوصٌ ذاتية   71/71                سموم        49/49
pytest        43                    ruff        نظيف
فجوةُ المرجع (DET)  335 ← 10 ← 6
نمط CVVCC الشاذّ      5 ←  3 ← 1
```

الفجوةُ ابتدأت ٣٣٥ وانتهت ٦، والستّةُ الباقية **ليست خللًا في المحرّك بل
تناقضًا في المرجع**: أسطحٌ لا لامَ تعريفٍ فيها يسمها `MASAQ` بـ`DET`، ومنها
`عُمُرَاً` التي تحمل `DET` و`CASE_INDEF` معًا في الكلمة الواحدة.

### ١ ـ٣ ما ينتظر حكمك — مرتَّبًا بالأثر

1. **عيبُ التنوين — ٣٬٠٥٩ كلمة.** أكبرُ خللٍ قائم. المعالجةُ المؤقّتة تفكّ
   التنوين إلى حركةٍ + نونٍ ساكنة ولا تحذف الألف الصامتة قبله في تنوين الفتح،
   فيولد **صامتٌ زائد**: `شَيْئَاً ← شَيْئَاَنْ` بخمسة صوامت لا أربعة،
   والمحور الثالث يقبله ولا يشكو.
   الصيغة المعروضة: «تنوينُ الفتح على ألفٍ صامتة ⟵ تُحذف الألف ويلحق التنوينُ ما قبلها».

2. **سبعةُ أصنافٍ غير مصادَقة** في `data/axis_1_owner_policy.json`. التصديقُ
   تغييرُ `false` إلى `true` في JSON، **لا تعديلُ سطرٍ في محرّك**:
   `U_TANWEEN` ٨٬٨٩٤ · `U_ALIF_FARIQA` ٣٬٥٦١ · `U_ALIF_MAQSURA` ٢٬٤٩٨ ·
   `U_N7_2_INTERNAL_AL` ١٬٦١٦ · `U_ALEF_MADDA` ١٬٥٠٥ · `U_UNVOCALIZED_CARRIER` ٦٠ ·
   `U_MULTIWORD_CELL` ٩.

3. **همزةُ الاستفهام الممدودة على غير «أل»** — صنفٌ ظهر أثناء N10.2 ولم يُحكم فيه.

4. **`OPEN_QUESTION_N2_2`** — وسمُ الشدّة بعد «ال»، ثلاثُ صيغٍ معروضة بلا اختيار.

5. **سجلّا المحور الثاني**: `operators_catalog` (١٦٠ مدخلة) و`mabniyat_full`
   (٥٦٥ مدخلة)، **مشكولان كما في النصّ**. الجردُ الحاليّ شاهدٌ مشتقّ (١٣٤ مدخلة)
   لا سجلُّ مالك، و`LICENSE_GRANTED = NO`.

6. **توسيعُ N10.2 إلى «آ» كلِّها؟** لم أوسّع: علّتُك («ء استفهام») لا تصدُق حيث
   لا استفهام، والتوسيعُ يُسقط المدَّ من ١٬٤١٨ موضعًا معجميًّا (`آمَنُوا ← ءَمَنُوا`).

---

## القسم الثاني — CL-16: سلسلة الجذر والوزن

### ٢ ـ١ المعمار — بحكمك

> «ليس أن ننقل كل هذه المشاريع داخل Hokom، ولا أن نغيّر Taaqol.
> الصحيح أن نجعلها مصادر أدلة مجمّدة: نثبت ملفاتها المهمة بالبصمة،
> ثم يقرأها Hokom عبر وسيط صغير، ويمنع أي ترقية غير مأذونة.»

كان قائمًا: المرحلةُ الأولى (اعتماد الجذر والباب) والثانية (تمرير الوزن)،
و**١٨ بصمةً كلُّها سليمة**. وكان الناقص أنّ المرحلة الأولى مفتاحُها **جذر**،
ولم يكن في CL-16 ما يُخرج جذرًا من سطح — رأسٌ بلا عنق.

### ٢ ـ٢ ما أُنجز: المرحلة الثالثة

`hokom/cl16_root_wazn/phase3/` — سلسلةٌ من ثلاث حلقات، كلُّ مصدرٍ في حدّه:

```
مرشح (أبو فريد) ← وجود (مقاييس) ← اعتماد وباب (audited_roots)
```

وسؤالُ مصدرٍ عمّا يمنعه حدُّه **حجبٌ لا تأجيل**: التأجيلُ إقرارٌ بحجّةٍ ناقصة،
وهذا خطأُ صنفٍ من أصله.

```
CORROBORATION_NEVER_RAISES_RANK
    الرتبةُ من جدول الاعتماد وحده. الترشيحُ والوجودُ تعضيدٌ: لكلٍّ أن يُنزل
    الحكمَ إلى تأجيل، وليس لأيٍّ منهما أن يرفع رتبةً درجةً واحدة.

SKELETON_MATCH_IS_NOT_A_ROOT_PROOF
    هيكلٌ صامتيّ يوافق صورةً في جرد Hokom المغلق: تأجيلٌ لا ترخيص.
```

**القياس على ٧٠٠ مدخلة**:

```
331 أجّله المستخرِج · 200 ليس في مقاييس · 67 بلا باب
 33 تصادمٌ مغلق     ·  33 أبوابٌ متعدّدة ·  23 غير معتمَد
 13 مرخَّص
```

المرخَّصةُ الثلاثَ عشرةَ: `غضب فعل فقد فهم قتل قول قوم كذب كلم` (مُثبَتة)
و`لحق لكم نصر نظر` (مدعومةٌ بدليل) — كلٌّ ببابه.

`17 اختبارًا يمرّ`. الحدود: `TAAQOL_CHANGED = NO` · `WEIGH_CALLED = NO` ·
`REPO_COPIED = NO` · `ROOT_PROVEN = NO` · `CL16_CLOSED = NO`.

### ٢ ـ٣ ما ينتظر حكمك

1. **`لكم` يُرخَّص وهو ليس جذرًا هنا**: أصلُه `لَكُمْ` (لام + ضمير)، وفي الجدول
   `لَكَمَ` (الضرب). الجردُ المغلق لا يمسكه لأن `لكم` فيه جزآن لا صورةٌ واحدة.
   **لم أخترع له قاعدة**؛ سُجِّل صنفًا مسمّى.

2. **نسخةٌ مكرّرة داخل Hokom**: `hokom/pipeline/taaqol_integration/maqayis_v2/`
   فيه المشروع كاملًا، وقاعدتُه مطابقةٌ بالبصمة للأصل **اليوم** (`35c7062dd5ed`).
   هي عينُ ما نهيتَ عنه، وقد تنحرف صامتةً غدًا.

3. **مُحوِّل `maqayis_v2` يفشل مفتوحًا** — «خطأ في المعجم لا يوقف الـ pipeline
   أبداً» — نقيضُ الفشل المغلق. ثُبِّت شاهدًا ولم يُستورد.

4. **المرحلتان ١ و٢ لا تُراجَعان إلا على جهازك**: مساراتُهما مطلقةٌ بلا تصريحٍ
   بمُركَّبٍ بديل. أُضيف المُحوِّل في الثالثة (`CL16_SOURCE_ROOT`، معطَّلٌ افتراضًا
   ومُثبَتٌ باختبار). وإضافتُه إليهما تعديلٌ في مرحلةٍ مشحونة — فهو حكمُك.

5. **`cl16_root_wazn/` غيرُ متتبَّعٍ في git بالكامل** (المراحل الثلاث)، وفي
   `hokom/.git/index.lock` قفلٌ عالق يمنع أيّ التزام.

---

## القسم الثالث — مثالٌ كامل: `كَتَبَ زَيْدٌ بِالْقَلَمِ`

جملةٌ واحدة مرّت على المحاور الخمسة. **كلُّ ما يلي مخرَجُ تشغيلٍ فعليّ**، منسوخٌ
كما طُبع، لا مُصاغٌ باليد.

```bash
printf '1|1|كَتَبَ زَيْدٌ بِالْقَلَمِ\n' > sentence.txt
aslot corpus --text sentence.txt --fragment --output-dir demo/axis0
aslot normalize --words demo/axis0/QURAN_WORDS.csv       --output-dir demo/axis1
aslot registry  --axis1-csv demo/axis1/AXIS_1_NORMALIZATION.csv --output-dir demo/axis2
aslot syllabify --axis1-csv demo/axis1/AXIS_1_NORMALIZATION.csv --output-dir demo/axis3
aslot peel      --axis1-csv demo/axis1/AXIS_1_NORMALIZATION.csv \
                --axis3-csv demo/axis3/AXIS_3_SYLLABLES.csv \
                --output-dir demo/axis4 --emit-masaq-like
```

### المحور ٠ — بناء النصّ · `QURAN_WORDS.csv`

```
Sura_No,Verse_No,Word_No,Word,Segment_Count,Segment_Numbers,Surface_Conflict,Segment_Numbering_Gap
1,1,1,كَتَبَ,1,1,NO,NO
1,1,2,زَيْدٌ,1,1,NO,NO
1,1,3,بِالْقَلَمِ,1,1,NO,NO
```

`FOLD_IS_LOSSLESS = YES` · فحوصٌ ٤/٤ · سمومٌ ٧/٧

### المحور ١ — التطبيع · `AXIS_1_NORMALIZATION.csv`

```
Sura_No,Verse_No,Word_No,Word,Normalized_Word,Normalization_Status,Owner_Decision_Classes,Rules_Applied,Stop_Reason,Jalalah_Prefix,Jalalah_Preserved
1,1,1,كَتَبَ,كَتَبَ,NORMALIZED,,,,,
1,1,2,زَيْدٌ,زَيْدُنْ,NORMALIZED_OWNER_DECISION_REQUIRED,U_TANWEEN,N12,,,
1,1,3,بِالْقَلَمِ,بِلْقَلَمِ,NORMALIZED_OWNER_DECISION_REQUIRED,U_N7_2_INTERNAL_AL,N7.2|N12,,,
```

* `كَتَبَ` لم تُمَسّ — لا قاعدةَ لها عمل.
* `زَيْدٌ ← زَيْدُنْ` بالتنوين المفكوك. **وهنا لا صامتَ زائد** لأن التنوين ضمٌّ
  على صامت؛ والعيبُ إنما يقع في تنوين الفتح على ألفٍ صامتة.
* `بِالْقَلَمِ ← بِلْقَلَمِ` — حُذفت همزةُ الوصل بـN7.2. واللامُ قمريّة (قاف)
  فبقيت ساكنة. ولو كانت شمسيّةً لعملت N9: `بِالشَّمْسِ ← بِشْشَمْسِ` (مقيس).

### المحور ٢ — العوامل والمبنيات · `AXIS_2_TOKENS.csv`

```
Sura_No,Verse_No,Word_No,Word,Normalized_Word,Closed_Form_Proof,Eligibility,Operator_Role,Word_Class,Next_Route,Matched_Entry_Ids,Note
1,1,1,كَتَبَ,كَتَبَ,NOT_MATCHED,BLOCK,NO,,CONTINUE,,
1,1,2,زَيْدٌ,زَيْدُنْ,NOT_MATCHED,BLOCK,NO,,CONTINUE,,
1,1,3,بِالْقَلَمِ,بِلْقَلَمِ,NOT_MATCHED,BLOCK,NO,,CONTINUE,,
```

لا شيءَ من الثلاث صورةٌ مغلقة، فالثلاثُ تمضي (`CONTINUE`).

### المحور ٣ — المقاطع الصوتية · `AXIS_3_SYLLABLES.csv`

```
Sura_No,Verse_No,Word_No,Word,Normalized_Word,Verdict,Syllable_Pattern,Consonant_Count,Madd_Status,Layn_Status,Reconstruction_Verified,Boundaries,Block_Reason
1,1,1,كَتَبَ,كَتَبَ,ACCEPT,CV·CV·CV,3,,,YES,0|2|4|6,
1,1,2,زَيْدٌ,زَيْدُنْ,ACCEPT,CVC·CVC,4,,PROVEN,YES,0|4|8,
1,1,3,بِالْقَلَمِ,بِلْقَلَمِ,ACCEPT,CVC·CV·CV·CV,5,,,YES,0|4|6|8|10,
```

الثلاثُ مقبولة، وكلُّها `Reconstruction_Verified = YES` — أي أُعيد بناءُ السطح
من مقاطعه وطوبق. و`زَيْدٌ` وحدها فيها لِينٌ مُثبَت (`Layn_Status = PROVEN`):
الياءُ الساكنة بعد فتحةٍ **صامتٌ في القفل لا امتدادُ نواة**.

### المحور ٤ — التقشير · `AXIS_4_PEEL_TO_STEM.csv`

```
Sura_No,Verse_No,Word_No,Word,Normalized_Word,Verdict,Termination,Peel_Count,Peeled_Prefixes,Licenses,Stem_Surface,Stem_Pattern,Stem_Consonants,Closed_Form_Proof,Deferred_Candidate_Prefix,Root_Work,Root_Proven,Stem_Proof,Note
1,1,1,كَتَبَ,كَتَبَ,ACCEPT,STEM_NOT_FURTHER_PEELABLE,1,كَ,LIC:4,تَبَ,CV·CV,2,NOT_MATCHED,,NONE,NO,NOT_CLAIMED,
1,1,2,زَيْدٌ,زَيْدُنْ,ACCEPT,STEM_NOT_FURTHER_PEELABLE,0,,,زَيْدُنْ,CVC·CVC,4,NOT_MATCHED,,NONE,NO,NOT_CLAIMED,
1,1,3,بِالْقَلَمِ,بِلْقَلَمِ,BLOCK,BLOCK_SYLLABLE_BOUNDARY_CROSSED,0,,,,,0,NOT_MATCHED,,NONE,NO,NOT_CLAIMED,القطع عند 2 يعبر حدًّا مقطعيًّا (CVC·CV·CV·CV)
```

### الجدول الجامع · `MASAQ_LIKE_OUTPUT.csv`

```
ID,Sura_No,Verse_No,Word_No,Segment_No,Word,Normalized_Word,Segment_Surface,Segment_Role,Peel_License,Syllable_Pattern,Consonant_Count,Closed_Form_Proof,Verdict,Termination,Morph_Type,Root
1,1,1,1,1,كَتَبَ,كَتَبَ,كَ,PREFIX,LIC:4,,,,ACCEPT,STEM_NOT_FURTHER_PEELABLE,,
2,1,1,1,2,كَتَبَ,كَتَبَ,تَبَ,STEM,,CV·CV,2,NOT_MATCHED,ACCEPT,STEM_NOT_FURTHER_PEELABLE,,
3,1,1,2,1,زَيْدٌ,زَيْدُنْ,زَيْدُنْ,STEM,,CVC·CVC,4,NOT_MATCHED,ACCEPT,STEM_NOT_FURTHER_PEELABLE,,
4,1,1,3,1,بِالْقَلَمِ,بِلْقَلَمِ,بِلْقَلَمِ,WHOLE_WORD,,CVC·CV·CV·CV,5,NOT_MATCHED,BLOCK,BLOCK_SYLLABLE_BOUNDARY_CROSSED,,
```

### ٣ ـ١ ماذا كشفت هذه الجملة — وهي ثلاثُ كلمات

**١. `كَتَبَ` قُشِّرت خطأً إلى `كَ + تَبَ`.**
الكافُ أصلٌ في «كتب» والمحرّك قشّرها. وهذا هو الحدُّ المسجَّل نفسُه
`T3_KNOWN_LIMIT_KAFARU_IS_WRONGLY_PEELED` (شاهدُه `كَفَرُوا`)، إلا أن وقوعَه هنا
**على الفعل النموذج نفسِه** أشدُّ دلالةً من شاهده الأصليّ.
والسبب مقيس: بوابةُ `INITIAL_LETTER_MAY_BE_RADICAL` تشترط نمط `CVV·CV·CV`،
و`كَتَبَ` نمطُها `CV·CV·CV` فلا تلتقطها البوابة. **حدٌّ معروف لا نقضٌ للمانع** —
والصنفُ أوسعُ من بوابته، وهذا مصرَّحٌ به في `README §٦`.

**٢. `بِالْقَلَمِ` حُجبت بالكامل ولم تُقشَّر.**
الباءُ سابقةٌ مرخَّصة (`بِ` في القائمة المغلقة)، ولكن N7.2 حذفت همزةَ الوصل
فالتصقت الباءُ باللام الساكنة وصار أوّلُ مقطعٍ `بِلْ` = `CVC`. والقطعُ عند
الموضع ٢ يعبر هذا المقطع، فالحكم `BLOCK` لا ترخيص. أي إنّ **قاعدةَ تطبيعٍ
غيرَ مصادَقة (`U_N7_2_INTERNAL_AL`، ١٬٦١٦ كلمة) تُسقط التقشير في المحور الرابع**.
وهذه كلفةٌ لم تكن ظاهرةً في الأرقام الإجمالية.

**٣. لا جذرَ ولا وزن — بالتصريح لا بالعجز.**
`Root` فارغ، و`Root_Work = NONE`، و`Root_Proven = NO`، و`Stem_Proof = NOT_CLAIMED`.
مسارُ الجذر مغلقٌ بحكمك، والجذعُ **وسمُ موقفٍ لا دعوى صرفية**.

### ٣ ـ٢ الوصلُ بـ CL-16 — ماذا يقول جذرُ «كتب»؟

المحور الرابع لا يخرج جذرًا. فإن سُئلت سلسلةُ CL-16 عن `كتب`:

```
كتب   | DEFER | MULTI_BAAB | rank=CANDIDATE | root=كتب | baab=None
   └─ ROOT_CANDIDATE_SOURCE    ROOT_CANDIDATE(NOT_A_PROVEN_ROOT)
   └─ MAQAYIS_LEXICON_SOURCE   PRESENT(PRESENCE_ONLY)
   └─ ROOT_PROOF_TABLE         DEFER:MULTI_BAAB
```

عبر الحلقاتِ الثلاثَ كلَّها ثم وقف عند الباب. والسبب في جدول الاعتماد:

```
4065  كَتَبَ | فَعَلَ يَفْعُلُ ( فتحُ ضمٍّ )   | مشترك | صحيح=1 أُعيد=1
4066  كَتَبَ | فَعَلَ يَفْعِلُ ( فتحُ كَسْرٍ ) | مشترك | صحيح=1 أُعيد=1
```

الجذرُ **مُثبَتٌ** رتبةً، وله **بابان**. فالوسيطُ لا يختار أحدَهما ولا يرجّح:
`DEFER` لا `BLOCK` — إقرارٌ بأنّ الحجّة ناقصة، لا دعوى بوجود مانع.
ولو سُئل عن `قتل` لرخّص: `CERTIFIED` ، `فَعَلَ يَفْعُلُ ( فتحُ ضمٍّ )`.

و`قلم` و`زيد` ليسا في جدول المرشحات أصلًا (`NOT_A_CANDIDATE`) — لأن المستخرِج
دُرّب على النصّ القرآنيّ، و`زيد` ليس منه.

---

## القسم الرابع — كيف يُستأنف العمل

```bash
# أسلوط
cd ~/final-september
pip install -e ".[dev]"     # أو  export PYTHONPATH=src
aslot all                   # السلسلة كاملةً على MASAQ.csv
make test && make lint

# CL-16
cd ~/hokom/cl16_root_wazn/phase3
python3 measure_chain.py --json PHASE3_MEASURES.json
python3 -m pytest tests/ -q
```

### المبادئ التي حكمت الجولة — وينبغي ألّا تُخرق

```
OWNER_RULE_SOVEREIGNTY = ABSOLUTE     CREATE_NON_OWNER_RULE = NO
INFER_LINGUISTIC_RULE  = NO           MEASURED_NOT_PRESET   = دائمًا
ACCEPT = علّةٌ حاضرة ∧ شروطٌ مستوفاة ∧ لا مانع
DEFER ≠ BLOCK: الحجبُ يدّعي مانعًا، والتأجيلُ يقرّ بحجّةٍ ناقصة
```

و**درسٌ مسجَّلٌ يستحقّ التكرار**: أوّلُ جردٍ للفظ الجلالة كُتب باليد فلم يطابق
منه إلا سطحٌ واحد من ٢٬٧٠٤ — لأن النصّ يكتب الشدّة قبل الحركة واليدُ تعكسهما.
فالقاعدة: **كلُّ سطحٍ مشكولٍ مكتوبٍ في ملفّ كودٍ أو اختبار غيرُ موثوقٍ بطبعه،
وما لم يُنقَل من مصدره فهو دعوى.**
