# سياسة U1: مجلدات الجولات مستبعدة من git
reports/**/*.csv
reports/axis_0_quran_build/QURAN_FROM_MASAQ.txt

# المدخل الخام: يُعرَّف ببصمته لا بنسخه
data/MASAQ.csv

# وسيطُ النقل والتنظيف — لا يدخل التاريخ
_to_delete/

# بايثون
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
build/
dist/
*.egg-info/
.venv/
venv/

# مستودعُ تعقُّل المُنزَل: يُقرأ ولا يُعدَّل، ويُنزَّل بأمرٍ لا يُنسخ في التاريخ
vendor/Taaqol-GPT/

# وسيطُ فكّ الحزمة — لا يدخل التاريخ
_stage/
.venv-taaqol/

# نقطةُ الرجوع قبل بسط التنوين — جداولُ محاورَ كاملة (71م).
# وسياسةُ U1 تُخرج `reports/**/*.csv` من التاريخ لعلّتها نفسِها، وهذه
# نسخةٌ منها. فتبقى على القرص شاهدًا يُقاس به، ولا تدخل التاريخ.
# ويُعاد بناؤها بأمرٍ: أُعِد الشيفرةَ إلى ما قبل التغيير وشغّل السلسلة.
.tanween_before/

# أرشيفاتُ النقل بين الحاوية والآلة — وسيطٌ لا أثر
*.tgz

# طبقاتٌ أقدمُ أُنقذت من الحاوية — تُحفظ على القرص، ولا تدخل التاريخ:
# لقطاتٌ كاملةٌ وجداولُ محاورَ (149م). والمانيفستُ فيها يُقرأ.
_container_history/
